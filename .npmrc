fetch-timeout=1800000
fetch-retries=10
fetch-retry-mintimeout=30000
fetch-retry-maxtimeout=300000
# Prefer IPv4 when IPv6 DNS exists but is unreachable (avoids pnpm fetch timeouts).
# Or run: NODE_OPTIONS="--dns-result-order=ipv4first" pnpm install --fetch-timeout=1800000
